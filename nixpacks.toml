[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd server && npm install --production"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd server && node src/app.js"
